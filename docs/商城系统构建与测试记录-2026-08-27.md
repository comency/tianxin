# 商城系统构建与测试记录（2026-08-27）

## 范围

- 企业店铺管理：后台 CRUD、权限、APP 店铺列表与详情。
- 商品归属店铺：SPU 关联店铺、后台选择、APP 商品按店铺筛选、启用状态校验。
- 商城管理端与 UniApp 移动端代码。

## 构建与测试结果

| 项目 | 命令/方式 | 结果 | 说明 |
| --- | --- | --- | --- |
| 后端打包 | `mvn -pl yudao-module-mall/yudao-module-product -am -DskipTests package` | 通过 | 21 个反应器模块均为 `SUCCESS`；耗时 29.424 秒。日志：`yudao-module-mall/yudao-module-product/target/mall-build-20260827.log`。 |
| 店铺服务单元测试 | `mvn -pl yudao-module-mall/yudao-module-product -am -Dtest=ProductShopServiceImplTest -Dsurefire.failIfNoSpecifiedTests=false test` | 通过 | 4 项测试通过：创建、重复企业校验、停用店铺校验、更新冲突校验；耗时 13.116 秒。日志：`yudao-module-mall/yudao-module-product/target/mall-shop-test-20260827.log`。 |
| 管理端生产构建 | `pnpm build:dev` | 通过 | Vite 构建成功，耗时 44.16 秒。存在既有 CSS minify 与大包体警告，未阻断构建。 |
| 管理端全量类型检查 | `pnpm ts:check` | 未通过（基线问题） | 6 个错误均位于既有 CRM/MES 页面，未涉及商城文件。 |
| 移动端 CLI 构建 | `pnpm exec vite build --mode development` | 未执行源码构建 | 移动端项目未声明 Vite/`@dcloudio/vite-plugin-uni` 依赖，当前 `node_modules` 不完整；应在 HBuilderX 或补全 UniApp 构建依赖后执行。 |
| 运行态全链路联调 | API + 管理端 + 移动端 + MySQL | 未执行 | 当前未提供已迁移数据库、可启动服务及测试账号；未将其记录为通过。 |

## 已知环境说明

- 图片上传在启用 Clash 时会出现“系统异常”；关闭 Clash 后可正常上传。该现象属于代理网络环境影响，当前不作为商城上传功能缺陷处理。后续联调应关闭 Clash 或将文件服务、后端接口域名配置为 `NO_PROXY` 后再验证。

## 结论

本次新增的后端商城店铺与商品归属代码已通过 Maven 打包和专项单元测试；管理端生产构建成功。发布前仍需在已执行 `mall-shop-2026-08-26.sql`、`mall-shop-enterprise-seed-2026-08-26.sql` 和必要修复脚本的测试库中，完成店铺创建、商品绑定、前台按店铺浏览与下单链路的人工联调。
