# Copy this file to application-local.yml to set machine-specific secrets.
# application-local.yml is deliberately excluded from Git.
spring:
  config:
    activate:
      on-profile: local
  datasource:
    url: jdbc:mysql://127.0.0.1:3306/tianxin_platform?useUnicode=true&characterEncoding=utf8&useSSL=false&serverTimezone=Asia/Shanghai
    username: root
    password: change-me

